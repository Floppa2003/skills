--- a/SKILL.md
+++ b/SKILL.md
@@ -129,25 +129,19 @@
 ### 3. LLM Validator (Edge Cases Only)
 
 ```python
+from collections.abc import Callable
+
 def validate_with_llm(
     item: ParsedItem,
     original_text: str,
-    client,
+    validate: Callable[[str], ParsedItem],
 ) -> ParsedItem:
-    """Use LLM to fix low-confidence extractions."""
-    response = client.messages.create(
-        model="claude-haiku-4-5-20251001",  # Cheapest model for validation
-        max_tokens=500,
-        messages=[{
-            "role": "user",
-            "content": (
-                f"Extract the question, choices, and answer from this text.\n\n"
-                f"Text: {original_text}\n\n"
-                f"Current extraction: {item}\n\n"
-                f"Return corrected JSON if needed, or 'CORRECT' if accurate."
-            ),
-        }],
+    """Use a provider-specific callback to fix low-confidence extractions."""
+    prompt = (
+        f"Extract the question, choices, and answer from this text.\n\n"
+        f"Text: {original_text}\n\n"
+        f"Current extraction: {item}\n\n"
+        "Return a ParsedItem. Preserve the current extraction unchanged if it is accurate."
     )
-    # Parse LLM response and return corrected item...
-    return corrected_item
+    return validate(prompt)
 ```
@@ -158,7 +154,7 @@
 def process_document(
     content: str,
     *,
-    llm_client=None,
-    confidence_threshold: float = 0.95,
+    llm_validator: Callable[[str], ParsedItem],
+    confidence_threshold: float,
 ) -> list[ParsedItem]:
     """Full pipeline: regex -> confidence check -> LLM for edge cases."""
@@ -168,7 +164,7 @@
     # Step 2: Confidence scoring
     low_confidence = identify_low_confidence(items, confidence_threshold)
 
-    if not low_confidence or llm_client is None:
+    if not low_confidence:
         return items
 
     # Step 3: LLM validation (only for flagged items)
@@ -176,7 +172,7 @@
     result = []
     for item in items:
         if item.id in low_conf_ids:
-            result.append(validate_with_llm(item, content, llm_client))
+            result.append(validate_with_llm(item, content, llm_validator))
         else:
             result.append(item)
 
